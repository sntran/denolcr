{
  "tasks": {
    "start": "deno run -A main.ts",
    "install": "deno install -A -n dclone main.ts",
    "bundle": "deno bundle -A main.ts",
    "test": "deno test -A --no-check --fail-fast",
    "bench": "deno bench -A"
  }
}
